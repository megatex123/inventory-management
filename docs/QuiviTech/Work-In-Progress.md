---
tags: [wip, status]
---

# Work In Progress

Last checked 2026-07-10 — `git status` clean except vault edits and Laravel Mix build artifacts (`public/js/app.js`, `package-lock.json` always diff after `npm run dev`/`watch`, not real changes). No in-flight code changes right now.

## Raw inventory / master SKU tracking — now COMPLETE

The subsystem described in earlier versions of this note (`ProductRaw`, `MasterSku`, `InvMove`, `InvCare`, `InvExclServe`) is fully built and working:
- Models: done (see [[Domain-Models]])
- Migrations: all 5 tables now have migration files (`database/migrations/2026_06_20_*`), and the live `quivi` DB's `migrations` table has them recorded — confirmed via a fresh local setup on 2026-07-10 (see [[Dev-Setup]])
- Controllers + routes: `master-sku`, `inv-care`, `inv-excl-serve` fully wired in `routes/api.php` (see [[API-Routes]])
- Vue CRUD: `resources/js/components/{master_sku,inv_care,inv_excl_serve}` (see [[Frontend-Components]])

`ProductRaw` and `InvMove` still have no controller/routes/Vue — this remains intentional (not in the original ERD), not a gap.

## Craft inspection + document management — built, not yet documented pre-2026-07-10

Both shipped after the vault's original 2026-07-02 snapshot and are now reflected in [[API-Routes]], [[Domain-Models]], and [[Frontend-Components]] as of this refresh. No known open gaps.

## ~~Known bug: `care_data`'s customer-name matching/sorting is silently broken~~ — FIXED 2026-08-01 (Batch 24) (found 2026-07-26)

`resources/js/components/care_data/index.vue` reads `care.customer.name` in 6 places — the 4 search/export-matching methods (`filteredCareData`, `applyClientSideFilters`, `getFilteredDataForExport`, `matchesFilters`) plus `sortCareData`'s `customer_name_asc`/`customer_name_desc` cases — but the API (`/api/care-data`) actually returns the field as `customer.full_name`, confirmed against this same file's own row-rendering template (`care.customer.full_name`, correct) and live curl. `care.customer.name` is always `undefined`, so:
- The "Care Details/Customer" search filter never matches on customer name (only `care_id`/email/customer_id still work).
- "Sort by Customer Name (A-Z/Z-A)" silently sorts everything as empty string (i.e. does nothing).

**Fixed in Batch 24 (2026-08-01).** All client-side matching/sorting methods that read `care.customer.name` (`filteredCareData`, `applyClientSideFilters`, `getFilteredDataForExport`, `matchesFilters`, `sortCareData`) were deleted outright rather than patched — the page now sends its search straight to the API's server-side `search` param, which already matched on `customers.full_name` correctly. The surviving `.name` reads that were genuinely still needed were renamed to `.full_name`: the Customer filter dropdown (`{{ customer.full_name }}`), the active-filter badge label, `generateFilterInfo()`'s customer line, and the two export renderers' `care.customer?.name` (which had the same bug and was silently printing `N/A` for every customer name in every exported report — a separate, previously-undocumented instance of this same bug found while fixing it).

Historical context: this predates the [[Frontend-Components]] "Collapsible Per-Column Search Panel" pilot (2026-07-26) — it was already present in the old blended search box this pilot replaced, and was carried forward faithfully (not introduced) since that pilot's plan required preserving existing matching logic verbatim. 

## Known bug: saving Performance Testing's top "Report Details" form blanks every section's displayed sub-form (found 2026-07-26)

`resources/js/components/performance_test/index.vue`'s `saveForm()` does `this.performanceTest = res.data.data` after a successful save, where `res.data.data` is `PerformanceTestController::update()`'s `$performanceTest->fresh()` — a fresh parent-model fetch with no child relations eager-loaded. This wipes `performanceTest.cpu_results`/`gpu_results`/`system_stability_results` (Phase 2) and `memory_results`/`storage_results`/`cooling_performance_results`/`cooling_system_results` (Phase 3) — all 7 keys become `undefined`. Every section component's `:initial-data="(performanceTest && performanceTest.xxx_results) || {}"` binding then receives `{}`, and each component's own `watch: { initialData(newVal) { this.form = this.buildForm(newVal); } }` rebuilds its form as blank.

Net effect: clicking "Save Report Details" (the top form, unrelated to any section) visually blanks every section's displayed values, even ones that were already saved. **No data is lost** — the DB rows are untouched, and reloading the page (`fetchData()`, which does eager-load all children) restores everything correctly — but it's a confusing UX regression a technician would hit constantly, since the top form and the 7 sections are both visible on the same page and there's no reason to expect saving one would visually clear the others.

Introduced in Phase 1/2 (the `saveForm()`/`performanceTest = res.data.data` pattern and the 3 Phase 2 sections' watchers), not by Phase 3 — but Phase 3 shipped 4 more sections following the identical pattern, broadening the blast radius from 3 sections to 7. Not yet fixed. Two candidate fixes, either applied once to `saveForm()`: (a) `Object.assign(this.performanceTest, res.data.data)` instead of full reassignment, since the parent response has no child keys to begin with, so merging preserves whatever child data is already loaded; or (b) have `PerformanceTestController::update()` eager-load the same 7 relations `show()` already does before returning `fresh()`. Either fixes all 7 sections at once — this is a single shared method, not something to patch per-section.

## Known gap: several inventory/BOM entities are named but unbuilt (found 2026-07-26)

(This heading was previously a stray duplicate of the `care_data` customer-name note above — the duplicate heading sat over this unrelated body. Corrected in Batch 24; the `care_data` bug itself is documented once, above, and is fixed.)

`INV_QVTD`, `INV_QVPL`, `INV_QVMR`, `INV_EXCL_QVMR`, and the `BOM_*` entities (`BOM_QVSE`, `BOM_QVPL`, `BOM_QVMR`, `BOM_DIS_QVMR`, `BOM_QVTD`) — no models, no tables, nothing built. Don't assume these are wanted; confirm with the user before building.

## Known bug: `suppliers/index.vue`'s "Shop Name (Z-A)" sort is a no-op (found 2026-07-27)

`sortSuppliers()`'s `shop_desc` case compares `b.shopname` to itself (`(b.shopname || '').localeCompare(b.shopname || '')`), which always returns `0` — the sort silently does nothing. Should be `(b.shopname || '').localeCompare(a.shopname || '')`, matching the `name_desc`/`code_desc` pattern used elsewhere in this and sibling pages (`brand`/`category`/`craft`/`sub_category`). Confirmed pre-existing (identical before the [[Frontend-Components]] "Collapsible Per-Column Search" Batch 1 migration touched this file) — found during that migration's final review, carried forward faithfully rather than fixed, since the migration's own scope was preserving existing filtering/sorting logic verbatim. Not yet fixed.

## Known bug: `care_warranties.i_qvca_id` doesn't store what its name implies (found 2026-07-27)

`Domain-Models.md`'s own documentation says `i_qvca_id` stores an InvCare business code (e.g. `IC-0001`) — true for 3 old manually-seeded rows, but not what the live create/edit UI does anymore. `resources/js/components/care_warranty/create.vue` and `edit.vue` both set `this.form.i_qvca_id = warranty.id.toString()`, where `warranty` comes from `ProductWarrantyController::getAvailableWarranties()`, which queries `product_warranties` — so as currently wired, new rows actually store a `product_warranties.id` raw integer PK as a string, not an InvCare code at all. Confirmed via `ProductWarrantyController.php`'s `getAvailableWarranties()`, which does a `whereNotIn('product_warranties.id', ...select('i_qvca_id')...)` subquery that only makes sense if `i_qvca_id` holds `product_warranties.id` values. Found during [[Business-ID-Normalization]]'s research (2026-07-27) — deliberately left out of that initiative's scope (it's a data-mapping bug, not a code-format inconsistency), not yet fixed.

## Known gap: `MenuItemsTableSeeder.php` is stale relative to the live sidebar (found 2026-07-27)

`database/seeds/MenuItemsTableSeeder.php`'s `run()` starts with `MenuItem::query()->delete()` and rebuilds the entire sidebar from its own hardcoded `$tree` array — but that array has no `Stock` top-level group at all, while the live `menu_items` table does (with an icon fix and items moved out of `Inventory`, shipped directly via `quivi.sql`/DB in commit `1945073` — "Stock menu icon fix + reorg" — which never touched this seeder file). Re-running `php artisan db:seed --class=MenuItemsTableSeeder` today would silently revert that reorg. Found while adding the [[QuiviRefund]] sidebar entry, which was done as a standalone additive migration (`2026_07_28_160000_add_refunds_menu_item.php`) instead of editing this seeder, specifically to avoid the same trap. Not yet fixed — fixing it means back-porting the live `Stock` group's actual shape (query `menu_items` for its current `parent_id`/children) into the seeder's `$tree` array, which nobody has done.

## List Page Standardization initiative — in progress (started 2026-07-29)

Rolling out standardized pagination/filtering/sorting (shared `PaginationControl`/`SortableTh`/`sortablePaginationMixin` components, `FiltersSortsAndPaginates` backend trait) across all list pages. See [[API-Routes]] for the per-page deviation notes this references. Shipped so far: shared components + mixin + hardening trait (interstitial infra batches), `care_data`, `brand`, `craft`, `category`, `sub_category`, `suppliers`, `care`, `product`, `stock`, `customer`, `meeting`, `meeting_details`, `uat_meeting`, `merch_items`, `merch_orders`, `thread_bom`, `thread_orders`, `plus_services`, `plus_orders`, `refunds`, `customer_progress`, `master_sku`, `inv_care`, `inv_excl_serve`, `inv_thread`, `inventory_movement`, `inv_merch`, `serve_data`. **The entire "meeting family" (`meeting`, `meeting_details`, `uat_meeting`) is now COMPLETE** (Batches 11-13, closed 2026-07-30). **The sidebar's "Stock" menu group (`stock`, `brand`, `category`, `sub_category`) is now COMPLETE** (Batch 14, closed 2026-07-30 — `stock/index.vue` switched from `GET /product/all` to `GET /product`, see [[API-Routes]]). **The sidebar's "QuiviMerch" menu group (`merch_items`, `merch_orders`) is now COMPLETE** (Batch 15, closed 2026-07-30 — both list pages migrated to the shared components, and both controllers' unvalidated-`orderBy()` sort column had allow-listed sorting added, closing the SQL-injection-shaped bug found while wiring up sorting for this batch). **The sidebar's "QuiviThread" menu group (`thread_bom`, `thread_orders`) is now COMPLETE** (Batch 16, closed 2026-07-30 — both list pages migrated to the shared components, and `ThreadBomController`/`ThreadOrderController` had the same allow-listed-sorting hardening applied. Note: `inv_thread` ("Thread Inventory") is a *different* page living under the sidebar's "Inventory" menu group, not "QuiviThread" — despite the similar name it is unrelated to this batch and remains unmigrated/out of scope). **The sidebar's "QuiviPlus" menu group (`plus_services`, `plus_orders`) is now COMPLETE** (Batch 17, closed 2026-07-30 — both list pages migrated to the shared components, and `PlusServiceController`/`PlusOrderController` had the same allow-listed-sorting hardening applied; `plus_orders` carries a 3-value status filter (`pending`/`scheduled`/`completed`) preserved unchanged through the migration). With Batch 17 closed, the unvalidated-`orderBy()` SQL-injection-shaped bug class is now fully closed across QuiviMerch (`MerchItemController`, `MerchOrderController`, Batch 15), QuiviThread (`ThreadBomController`, `ThreadOrderController`, Batch 16), and QuiviPlus (`PlusServiceController`, `PlusOrderController`, Batch 17) — 6 controllers total. **`care_warranty` is now COMPLETE** (Batch 18, closed 2026-07-30 — `CareWarrantyController` gained the same allow-listed-sorting hardening plus a new `all()` endpoint (fixing 3 of the page's 4 export paths, which previously 404'd), and `care_warranty/index.vue` was migrated to the shared `PaginationControl`/`SortableTh`/`sortablePaginationMixin` stack; the page's lengthy pre-existing styled-Excel/CSV export functionality was preserved verbatim aside from the sort param rename). This is the first of the two remaining QuiviCare pages — `care_data` is deliberately deferred to its own future batch given its much larger scope and known pre-existing bugs. Batch 18 also found a **4th distinct instance** of the unvalidated-`orderBy()` bug class, using yet another param-name variant (`sort_field`/`sort_direction`, distinct from the `order_by`/`order_direction` instances fixed in Batches 15-17) — worth actively checking for in each remaining unmigrated page, since the naming isn't consistent across the codebase. **`refunds` is now COMPLETE** (Batch 19, closed 2026-08-01 — `RefundController` gained the same allow-listed-sorting hardening (`sort_by`/`sort_dir`) plus a new `/statistics` endpoint, and `refunds/index.vue` was migrated to the shared `PaginationControl`/`SortableTh`/`sortablePaginationMixin` stack; the 4 pre-existing statistics cards and the Filters & Search card were preserved unchanged). With Batch 19 closed, **the sidebar's "Refund" menu group is now fully migrated**, and the unvalidated-`orderBy()`/sort-param SQL-injection-shaped bug class is now fixed across QuiviMerch, QuiviThread, QuiviPlus, and Refund — 7 controllers total (Batches 15/16/17/19). **`customer_progress` is now COMPLETE** (Batch 20, closed 2026-08-01 — `CustomerProgressController` gained the same allow-listed-sorting hardening (`sort_by`/`sort_dir`), and `customer_progress/index.vue` was migrated to the shared `PaginationControl`/`SortableTh`/`sortablePaginationMixin` stack; the 4 pre-existing statistics cards and the Filters & Search card were preserved unchanged). With Batch 20 closed, **the sidebar's "Customer Progress" menu group is now fully migrated**, and the unvalidated-`orderBy()`/sort-param SQL-injection-shaped bug class is now fixed across 8 controllers total: QuiviMerch, QuiviThread, QuiviPlus, Refund, and `customer_progress` (Batches 15-17, 19-20), plus the differently-named `sort_field`/`sort_direction` instance in `care_warranty` (Batch 18). **`master_sku`, `inv_care`, and `inv_excl_serve` are now COMPLETE** (Batch 21, closed 2026-08-01 — `MasterSkuController`, `InvCareController`, and `InvExclServeController` each gained the same allow-listed-sorting hardening (`sort_by`/`sort_dir`) plus a `/statistics` endpoint, and all three list pages (`master_sku/index.vue`, `inv_care/index.vue`, `inv_excl_serve/index.vue`) were migrated to the shared `PaginationControl`/`SortableTh`/`sortablePaginationMixin` stack; each page's pre-existing statistics cards and Filters & Search card were preserved unchanged. `inv_excl_serve` had 0 live rows at verification time — confirmed via live curl returning an empty `data:[]` with correct `meta` shape). This covered **3 of the 5 remaining Inventory-group pages** at the time — `inv_thread` and `inventory_movement` were still pending and deliberately deferred to a follow-up batch given Batch 21's already-large scope (3 pages × backend+frontend = 6 tasks). **`inv_thread` and `inventory_movement` are now COMPLETE** (Batch 22, closed 2026-08-01 — `InvThreadController` and `InventoryMovementController` each gained the same allow-listed-sorting hardening (`sort_by`/`sort_dir`), and both list pages (`inv_thread/index.vue`, `inventory_movement/index.vue`) were migrated to the shared `PaginationControl`/`SortableTh`/`sortablePaginationMixin` stack; `inventory_movement`'s deliberate `date`/`desc` default sort — distinct from the majority `created_at`/`desc` pattern used elsewhere — was preserved as-is rather than "corrected" for consistency, since it's the existing, intentional behavior for this page. `InventoryMovementController`'s pre-existing lack of a `search()` method was likewise left untouched). **With Batch 22 closed, `master_sku`, `inv_care`, `inv_excl_serve` (Batch 21), `inv_thread`, `inventory_movement` (Batch 22), and `product`/`stock` (Batch 14 and earlier) are all migrated.** Correction (found during Batch 22's final whole-batch review): the sidebar's "Inventory" menu group (live `menu_items` group id 77) actually had **7** list pages, not the 6 counted above — it also contains `inv_merch` ("QuiviMerch Inventory"), which at that point was still unmigrated (see [[API-Routes]]'s `inv_merch`/`inv_excl_merch` merge note for history). **`inv_merch` is now COMPLETE** (Batch 23, closed 2026-08-01 — `InvMerchController` gained the same allow-listed-sorting hardening (`sort_by`/`sort_dir`, allow-listed to `inv_merch_id`/`item_name`/`sku_code`/`unit_cost`/`created_at`), and `inv_merch/index.vue` was migrated to the shared `PaginationControl`/`SortableTh`/`sortablePaginationMixin` stack; the pre-existing statistics cards and the `is_exclusive` Filters & Search card were preserved unchanged). **With Batch 23 closed, the sidebar's entire "Inventory" menu group is now fully migrated — all 7 pages (`master_sku`, `inv_care`, `inv_excl_serve`, `inv_thread`, `inventory_movement`, `inv_merch`, `product`) use the shared pagination/filtering/sorting stack.** The unvalidated-`orderBy()`/sort-param SQL-injection-shaped bug class is now fixed across **15 controllers total**: QuiviMerch's `MerchItemController`/`MerchOrderController`, QuiviThread's `ThreadBomController`/`ThreadOrderController`, QuiviPlus's `PlusServiceController`/`PlusOrderController`, `RefundController`, `CustomerProgressController` (Batches 15-17, 19-20), `CareWarrantyController`'s `sort_field`/`sort_direction` variant (Batch 18), `MasterSkuController`/`InvCareController`/`InvExclServeController` (Batch 21), `InvThreadController`/`InventoryMovementController` (Batch 22), and `InvMerchController` (Batch 23). **`care_data` (QuiviCare) is now COMPLETE** (Batch 24, closed 2026-08-01 — `CareDataController@index` gained hardened allow-listed sorting plus `CAST`-based numeric sorting for the `varchar` `price`/`total_part` columns, dead `status`/`appointment_date` filter paths removed, and search LIKE escaping; see [[API-Routes]] for the full backend detail. `care_data/index.vue` was migrated to the shared `PaginationControl`/`SortableTh`/`sortablePaginationMixin` stack). **Unlike every prior batch in this initiative, `care_data`'s migration fixed a REAL, user-facing data-corruption bug, not just a cosmetic sort/filter issue**: the page's `filteredCareData` computed property re-filtered, re-sorted, AND re-`.slice()`d the server's already-paginated response, so on page 2+ it was slicing `[(page-1)*perPage, page*perPage]` out of an array that only ever held ≤`per_page` rows — page 2 rendered empty, page 3+ was permanently empty, and the row counter showed the current page's length as if it were the whole result set. All of that client-side machinery (`filteredCareData`, `sortCareData`, `applyClientSideFilters`, `getFilteredDataForExport`, `matchesFilters`, the hand-rolled `pages`/`lastPage` computeds and `changePage()`) was deleted; the page now trusts the server's `meta` block. The page's ~500 lines of styled-Excel/CSV export rendering were preserved verbatim; only their *data acquisition* changed (they now re-fetch from the API with the current filters instead of re-filtering a single page client-side, which had been silently truncating exports to one page's worth of rows — note the export request is still clamped to 100 rows by the shared `resolvePerPage()` max, so it is "all matching rows up to 100", not literally all; see follow-up 3 in [[API-Routes]]). A follow-up fix during this batch's review also corrected the year/month filter, which built its `start_date`/`end_date` via `Date.toISOString()` — a UTC conversion that shifted the whole range back a day in this UTC+8 deployment (July would have been requested as 2026-06-30 … 2026-07-30); it now formats from the local-time `getFullYear()`/`getMonth()`/`getDate()` components. The 4 free-text column filters (`care_customer`/`order`/`total_part`/`price`) were collapsed into the API's single `search` param, and `date_from`→`created_from`, `year`/`month`→`start_date`/`end_date`, because none of the old client-only filter names existed server-side. The header stat cards now read the API's `summary` block (totals across ALL matching rows) instead of summing the current page. **`care_data` is NOT "fully fixed" — only its pagination/sort/filter/export layer is.** `update()`/`updateStatus()`/`destroy()`/`restore()` still write to columns that don't exist in the live table, and several of those methods have no route registered at all; this was an explicit, recorded scoping decision for Batch 24 and remains unfixed — see the `care_data` paragraph in [[API-Routes]] for the full list. **`serve_data` (QuiviServe) is now COMPLETE** (Batch 25, closed 2026-08-02 — `ServeDataController@index` was ported to the shared `FiltersSortsAndPaginates` trait with `sort_by` allow-listed to `created_at`/`serve_id` plus a hand-kept join-based `customer_name` special case (the trait's flat allow-list can't express joined-column sorts); `serve_data/index.vue` was migrated to the shared `PaginationControl`/`SortableTh`/`sortablePaginationMixin` stack, replacing a 100% hand-rolled pagination `<ul>` and a `sortBy` `<select>` dropdown. Unlike `care_data` (Batch 24), **this page had no double-pagination bug** — its main table rendered the fetched server page directly; the client-side `matchesFilters()`/`getFilteredDataForExport()`/`sortServes()` machinery was verified to be reachable only from the CSV/styled-Excel export paths and was preserved. The one unavoidable touch was `sortServes()`'s switch expression, which read the removed `filters.sortBy`; it now reads a new `exportSortBy` computed derived from the table's `sortState`, so exports follow the visible sort. The combined "Serve Details / Customer" column was split into two columns so `serve_id` and `customer_name` could each get their own `SortableTh` (`SortableTh` renders a `<th>`, so two sort keys can't share one header cell). The never-implemented `package_price` sort option was deliberately NOT added to the backend. **`serve_data` is NOT "fully" fixed — only its pagination/sort/filter layer is standardized.** Its missing/broken routes (`search`/`byCustomer`/`byOrder`/`restore`, and a private `exportToCSV`) and its dead `serve.notes`/`serve.upgrade_price` field references remain out of scope and unfixed, per the project owner's recorded decision. Found during Batch 25's final review, not fixed (cosmetic, pre-existing, not a regression): the "Showing X to Y of Z" count now renders twice — once in a card-header (which also carries a `for "search term"` annotation `PaginationControl` has no equivalent for) and once from `PaginationControl` itself; and the export modal's "filtered" vs. "all" row-count labels always show the same number, since both were already wired to the same `meta.total`-derived value before this batch.) This is the **first page of the QuiviServe family**; `serve_bek`, `serve_mps`, and `serve_pce` were still pending at that point. **`serve_bek` is now COMPLETE** (Batch 26, closed 2026-08-02 — `ServeBekController@index` was ported to the shared `FiltersSortsAndPaginates` trait with `sort_by` allow-listed to `created_at`/`date_start`; `serve_bek/index.vue` was migrated to the shared `PaginationControl`/`SortableTh`/`sortablePaginationMixin` stack, replacing a 100% hand-rolled `<ul class="pagination">` footer and a `paginationRange()` computed. The page's inline "click to claim a perk" UX (`makeClaim()`) patches the affected row in place after a successful claim rather than refetching the whole list; this was traced and updated to write into the new `items` array (`this.items.findIndex`/`this.items.splice`) instead of the old `this.serveBeks.data` paginator-nested array. Task 1's backend response-shape change (raw paginator object under `data` → `{success, data:[...], meta:{...}}`) was confirmed self-contained: `serve_bek/index.vue` is the only consumer of `GET /api/serve-beks` in the codebase.). **`serve_mps` is now COMPLETE** (Batch 27, closed 2026-08-02 — `ServeMpsController@index` gained allow-listed sorting (`sort_by` limited to `created_at`/`date_start`, via the shared `FiltersSortsAndPaginates` trait); `serve_mps/index.vue` was migrated to the shared `PaginationControl`/`SortableTh`/`sortablePaginationMixin` stack. **Like `care_data` (Batch 24) and unlike `serve_data`/`serve_bek`, this page had a REAL client-side double-pagination bug**: its `filteredServeMps` computed re-filtered the server's already-paginated response and `paginatedServeMps` then re-`.slice()`d that filtered array whenever any filter was active, so filtering silently showed a slice-of-a-slice against a one-page-deep dataset and the "Showing X of Y" counter reported page-local numbers as if they were the whole result set. Both computeds were deleted and all five filters (`qvse_cid`, `warranty_status`, `promo_status`, `date_start_from`, `date_start_to`) are now sent to the server as query params. Note the `promo_status` dropdown's `available` option value was renamed to `generated`, because the controller's `generated` branch (`generate_code && !rm100_promo_code_claim`) is the exact equivalent of the old client-side `available` predicate, while the controller's own `available` branch means something different (promo-code string present) — renaming preserved the page's previous behaviour rather than silently changing it. **A backend fix outside the planned scope was required:** the controller's `qvse_cid` filter did `where('qvse_cid', ...)` directly on `serve_mps`, but `qvse_cid` is **not a column on that table** — it is an accessor (`ServeMps::getQvseCidAttribute`) that reads through the `serveData` relation. Any request carrying that filter returned a 500 (`SQLSTATE[42S22] Unknown column 'qvse_cid' in 'WHERE'`). Because the frontend refactor routes that filter to the server for the first time, it had to be fixed or the migration would have traded a double-pagination bug for a hard 500; it now uses `whereHas('serveData', ...)`. This bug was invisible before precisely *because* the filter was client-side. The page also carried **2 previously-undocumented instances of the promo-code-string-vs-boolean rendering bug** — `showQuickInfo()`'s "Promo Code" line and `exportToExcel()`'s promo-code CSV column both read the boolean `rm100_promo_code_claim` where the string `rm100_promo_code_next_build` was intended (so both rendered `true`/blank instead of the code); a prior partial fix referenced in [[QuiviServe]] had corrected only the main table's Promo Code column, which was verified untouched here. Also removed: a redundant second full `GET /api/serve-mps` call (`fetchStatistics()`) and a `calculateStatistics()` helper that recomputed the stat cards from a single page's rows — the index endpoint already returns whole-table `statistics`, which the page now reads directly. A new sortable "Created" column was added to the table so both allow-listed sort keys are reachable. **`serve_mps` is NOT fully fixed — only its pagination/sort/filter layer plus the two rendering bugs.** Three pre-existing broken/missing routes remain deliberately unfixed: `statistics`, `search`, and `mark-promo-claimed` (the last 404s, so the "Mark Claimed" button has never worked; its in-place row patch `item.rm100_promo_code_claim = true` was traced and preserved through the array changes but is unreachable until the route exists). `exportToExcel()` still exports only the current page despite its "Export All" label — pre-existing, unchanged, not a regression.) **3 of 4 QuiviServe pages are now done: `serve_data`, `serve_bek`, `serve_mps`.** **`serve_pce` is now COMPLETE** (Batch 28, closed 2026-08-02 — `ServePceController@index` gained allow-listed sorting (`sort_by` limited to `created_at`/`date_start`, same convention as `serve_bek`/`serve_mps`); `serve_pce/index.vue` was migrated to the shared `PaginationControl`/`SortableTh`/`sortablePaginationMixin` stack, replacing a hand-rolled `<ul class="pagination">` footer and the never-read `sortField`/`sortDirection` data props. **Unlike `care_data` (Batch 24) and `serve_mps` (Batch 27), this page needed no live-bug fixes** — its `filteredServePces`/`paginatedServePces` computeds were explicitly checked (not just assumed from research) and confirmed genuinely dead in practice: `paginatedServePces`'s length-guard (`servePces.length <= itemsPerPage`) always short-circuited to the raw server page in normal use, since the fetched array is never larger than one page, so the re-slice branch was unreachable; both computeds were deleted outright. The promo-code rendering at all three locations (table, quick-info modal, CSV export) was likewise explicitly checked against the `rm100_promo_code_claim`-vs-string bug pattern found in `serve_mps` and confirmed already correct: all three read `item.promo_code` (string) separately from `item.promo_claim`/`item.generate_code` (booleans). This was a clean sort-support-plus-component-wiring migration with two potential bug classes checked-and-confirmed-absent rather than silently assumed fine. A new sortable "Created" column was added alongside "Start Date" so both allow-listed sort keys are reachable.) **This completes the entire QuiviServe family — all 4 pages (`serve_data`, `serve_bek`, `serve_mps`, `serve_pce`, Batches 25-28) are now fully migrated to the shared pagination/filtering/sorting stack**, matching the "fully migrated" phrasing used for the meeting, Stock, QuiviMerch, QuiviThread, QuiviPlus, Refund, Customer Progress, and Inventory menu groups above. **`order.vue` ("Today's Orders") gained server-side sort support** (Batch 29, closed 2026-08-02) — deliberately NOT the full pagination treatment other pages get, since its stat cards depend on the complete day's dataset and the natural data volume doesn't warrant real pagination; see [[API-Routes]] for the full reasoning. First of two Orders-pages batches — `allorder.vue` ("All Orders") remains pending, deferred to its own larger follow-up batch given its much richer UI (stat cards, export, approve/reject actions, quick-launch links) and the shared `GET /api/orders` endpoint's 6 external consumers requiring a new dedicated endpoint rather than a direct conversion. **`serves` (the QuiviServe tier lookup, not to be confused with the `serve_data`/`serve_bek`/`serve_mps`/`serve_pce` family above) had its ad-hoc "Sort By" dropdown replaced with `SortableTh` column-header sorting** (Batch 30, closed 2026-08-02). Unlike every other page in this initiative, this one deliberately did NOT get `PaginationControl` or the backend `FiltersSortsAndPaginates` trait — `serves` is a fixed 3-row reference table (Essential Kit / Prime Series / Collector's Edition) fetched whole in a single `GET /api/serves` call, so server-side pagination/sorting infrastructure would add complexity for no benefit; sorting stays entirely client-side against the one already-fetched array, driven by a `sortState`/`onSort()` pair matching the pattern `SortableTh` uses everywhere else. This batch also fixed a genuine, previously-undocumented bug in `sortServes()`: the `'name'` case (and its `default` fallthrough) sorted by `parseFloat(a.fee) - parseFloat(b.fee)` instead of by name — the old dropdown's "Name (A-Z)" option never actually sorted alphabetically, it silently sorted by fee ascending. The replacement switch now genuinely sorts by name via `localeCompare`. In progress/next: none currently in-flight. Still pending: `employees`/`salaries` (currently empty tables in the dev DB), `expenses`, and the remainder of the ~37-page inventory.

**Known bug (found 2026-07-29, pre-existing, unrelated to this initiative): 8 pages call a `/api/customers` (plural) route that doesn't exist.** `thread_orders/{create,edit}.vue`, `plus_orders/{create,edit}.vue`, `merch_orders/{create,edit}.vue`, and `refunds/{create,edit}.vue` all do `axios.get('/api/customers', { params: { per_page: 1000 } })` for their customer-picker dropdowns. No such route is registered in `routes/api.php` (the real endpoint is singular, `/api/customer`) — live, the request returns HTTP 200 with the SPA's own HTML shell (Laravel's catch-all frontend route), and each page's `res.data.data || res.data` fallback then assigns that HTML string to its customer list, silently breaking the dropdown. Discovered while grepping the whole tree for `/api/customer` consumers during the List Page Standardization initiative's Batch 10 final review — not caused by that batch, and never worked. The `per_page: 1000` shape suggests these were written expecting a paginated `/api/customers` that was never built. Cheap fix now that `GET /api/customer/all` exists (added in Batch 10): repoint all 8 to `/api/customer/all` and drop the now-unnecessary `per_page: 1000` param. Not yet fixed.

**Resolved (Batch 14, 2026-07-30):** `resources/js/components/stock/index.vue` — the second, separately-routed list page (`/product/stock`, "Stock List") discovered during the `product` batch was migrated to the shared `PaginationControl`/`SortableTh`/`ColumnSearchPanel`/`sortablePaginationMixin` stack, consuming the already-paginated `GET /api/product` (no backend changes needed). See [[API-Routes]] for the consumer-change note.

## Related
- [[Domain-Models]]
- [[API-Routes]]
- [[Architecture]]
- [[Dev-Setup]]
- [[Business-ID-Normalization]]
